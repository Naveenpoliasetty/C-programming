#include <stdio.h>

int main()
{
    int num, i;
    printf("enter the number \n");
    scanf("%d", &num);
    for (i = 1; i<= num; i++)
    {
        printf("%d",i);
    }
     //printf ("the sum is %d",sum);
    return 0;
}